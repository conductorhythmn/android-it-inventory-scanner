# android-it-inventory-scanner
A QR code and barcode scanner for Android, built for fast bulk scanning of physical inventory. It scans and exports directly to CSV, XLSX, PDF, XML, JSON, or a custom webhook/API endpoint — no manual data entry required.
